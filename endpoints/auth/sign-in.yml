info:
  name: sign-in
  type: grpc
  seq: 1

grpc:
  url: "{{http}}://{{host}}:{{authPort}}"
  method: /auth.v1.AuthService/SignIn
  methodType: unary
  protoFilePath: ../neuraclinic-proto-contracts/proto/auth/v1/auth.proto
  metadata:
    - name: accept-language
      value: "{{language}}"
  message: |-
    {
      "email": "test@email.com",
      "password": "Test1324*"
    }
  auth: inherit
