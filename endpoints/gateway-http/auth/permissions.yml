info:
  name: permissions
  type: http
  seq: 11

http:
  method: GET
  url: "{{http}}://{{host}}:{{gatewayPort}}/api/v1/auth/permissions"
  headers:
    - name: X-Auth-Mode
      value: mobile
      disabled: true
    - name: Authorization
      value: Bearer {{gatewayAccessToken}}
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
