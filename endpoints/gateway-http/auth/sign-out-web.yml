info:
  name: sign-out-web
  type: http
  seq: 4

http:
  method: POST
  url: "{{http}}://{{host}}:{{gatewayPort}}/api/v1/auth/sign-out"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {}

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
