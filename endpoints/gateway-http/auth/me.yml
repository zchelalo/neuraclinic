info:
  name: me
  type: http
  seq: 7

http:
  method: GET
  url: "{{http}}://{{host}}:{{gatewayPort}}/api/v1/auth/me"
  headers:
    - name: X-Auth-Mode
      value: mobile
      disabled: true
    - name: Authorization
      value: Bearer {{gatewayAccessToken}}
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
