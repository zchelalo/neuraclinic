info:
  name: list
  type: http
  seq: 2

http:
  method: GET
  url: "{{http}}://{{host}}:{{gatewayPort}}/api/v1/users?limit=2"
  headers:
    - name: X-Auth-Mode
      value: mobile
      disabled: true
    - name: Authorization
      value: Bearer {{gatewayAccessToken}}
      disabled: true
  params:
    - name: limit
      value: "2"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
