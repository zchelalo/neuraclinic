info:
  name: find-by-email
  type: http
  seq: 4

http:
  method: GET
  url: "{{http}}://{{host}}:{{gatewayPort}}/api/v1/users/by-email?email=test4@email.com"
  headers:
    - name: X-Auth-Mode
      value: mobile
      disabled: true
    - name: Authorization
      value: Bearer {{gatewayAccessToken}}
      disabled: true

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
