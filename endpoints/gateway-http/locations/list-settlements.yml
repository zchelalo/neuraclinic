info:
  name: list-settlements
  type: http
  seq: 4

http:
  method: GET
  url: "{{http}}://{{host}}:{{gatewayPort}}/api/v1/locations/settlements?country_code=MX&admin_area_code=14&postal_code=44100&limit=5&query=centro"
  headers:
    - name: X-Auth-Mode
      value: mobile
      disabled: true
    - name: Authorization
      value: Bearer {{gatewayAccessToken}}
      disabled: true
  params:
    - name: country_code
      value: MX
      type: query
    - name: admin_area_code
      value: "14"
      type: query
    - name: postal_code
      value: "44100"
      type: query
    - name: limit
      value: "5"
      type: query
    - name: query
      value: centro
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
