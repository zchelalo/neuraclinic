info:
  name: metrics
  type: http
  seq: 2

http:
  method: GET
  url: "{{http}}://{{host}}:{{gatewayPort}}/api/metrics"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
