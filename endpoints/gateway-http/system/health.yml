info:
  name: health
  type: http
  seq: 1

http:
  method: GET
  url: "{{http}}://{{host}}:{{gatewayPort}}/api/health"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
