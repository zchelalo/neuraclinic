info:
  name: update-password
  type: grpc
  seq: 6

grpc:
  url: "{{http}}://{{host}}:{{userPort}}"
  method: /user.v1.UserService/UpdatePassword
  methodType: unary
  protoFilePath: ../../neuraclinic-api-client-protos/user/v1/user.proto
  metadata:
    - name: accept-language
      value: "{{language}}"
    - name: x-user-id
      value: "{{userId}}"
      disabled: true
    - name: x-internal-service-token
      value: "{{internalServiceToken}}"
  message: |-
    {
      "id": "{{userId}}",
      "new_password": "Test1324*"
    }
  auth: inherit
